-- The orbit_jobs metadata table is auto-created by the engine on startup;
-- this file exists so that future schema evolution can use sqlx-cli migrations.
-- Add new migrations here once a real schema is needed beyond engine bootstrap.

-- (Intentionally empty. Engine::open creates orbit_jobs idempotently.)
